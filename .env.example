# Public builds use demo mode and must not receive provider credentials.
APP_MODE=demo

# Private mode only. Configure these in the protected environment, never git.
DATABASE_URL=postgresql://user:password@host/database?sslmode=require
OPENAI_API_KEY=replace-in-private-environment-only
AI_ANALYSIS_ENABLED=false
